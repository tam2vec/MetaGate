{
  "mcpServers": {
    "predicate": {
      "command": "predicate-mcp",
      "args": [
        "--policy",
        "examples/policies/enterprise_ai.yml",
        "--datahub-url",
        "http://localhost:8080/api/graphql"
      ],
      "env": {
        "DATAHUB_TOKEN": "${DATAHUB_TOKEN}"
      }
    }
  }
}
