services:
  - type: web
    name: predicate-review-api-live
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: PREDICATE_DEMO_MODE
        value: live
      - key: DATAHUB_GRAPHQL_URL
        sync: false
      - key: DATAHUB_TOKEN
        sync: false
