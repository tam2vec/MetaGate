services:
  - type: web
    name: predicate-review-api
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: PREDICATE_DEMO_MODE
        value: fixture
      - key: DATAHUB_GRAPHQL_URL
        value: ""
