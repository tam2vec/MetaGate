services:
  - type: web
    name: metagate-review-api
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    envVars:
      - key: METAGATE_DEMO_MODE
        value: fixture
      - key: DATAHUB_GRAPHQL_URL
        sync: false
      - key: DATAHUB_TOKEN
        sync: false
